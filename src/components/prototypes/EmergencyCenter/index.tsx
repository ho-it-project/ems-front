import { EmergencyCenterContainer } from "@/components/container/EmergencyCentainer";

export const EmergencyCenterPrototype = () => {
  return <EmergencyCenterContainer />;
};

/**
 *
 *
 * 관계 정립해놓지않음?
 *
 * 그럼 한번에 만들수있을텐디
 *
 *
 * 이미지 먼저 만든다는게
 * 
 * s3에 미리 업로드 -> 이미지 별로 url을 받아서
 * -> 게시글이랑 한번에 생성
 * 
 * 
 * 
 */
